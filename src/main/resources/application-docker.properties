# SQLite Database Configuration para Docker
spring.datasource.url=jdbc:sqlite:database.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# SQL Schema Configuration
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# Logging Configuration para Docker - Solução simples sem XML
# Logs personalizados APENAS no arquivo
logging.file.name=/app/logs/personalized-logs.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# Console completamente limpo - sem logs
logging.pattern.console=
logging.level.ROOT=OFF
# Logs personalizados APENAS no arquivo
logging.level.br.com.testedit.teste_tecnico_dti=INFO
